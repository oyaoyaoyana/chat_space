= form_for(chat_group) do |f|
  - if chat_group.errors.any?
    #error_explanation
      %h2
        = pluralize(chat_group.errors.count, "error")
        prohibited this chat_group from being saved:
      %ul
        - chat_group.errors.full_messages.each do |message|
          %li= message
  .actions
    = f.submit
